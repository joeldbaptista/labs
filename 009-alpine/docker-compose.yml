services:
  uzbox:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: uza
        PASSWORD: uza1234
    container_name: uzbox
    tty: true # Ensure a TTY is allocated

